@page "/Repos"

<div class="PageWrapper">
    @if (selectedItem != null)
    {
        <ModelEditor Model="selectedItem"
                     CancelRequest="@(()=>selectedItem = null)"></ModelEditor>
    }
    else
    {
        <h1>Repositoris</h1>
        <Repos ItemSelected="ItemSelectedHandler"></Repos>
    }
</div>

@code {
    object? selectedItem;

    void ItemSelectedHandler(object item)
    {
        selectedItem = item;
    }
}