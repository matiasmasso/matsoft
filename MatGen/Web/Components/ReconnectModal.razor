@code {
    private bool showReconnect = false;
    // Add logic to toggle visibility and handle reconnect events
}

<div class="reconnect-modal" hidden="@(!showReconnect)">
    Attempting to reconnect...
</div>
