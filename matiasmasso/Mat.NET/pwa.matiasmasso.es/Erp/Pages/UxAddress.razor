@inherits _ComponentBase

<div class="Wrapper">
    @if (CanEdit && isEditing)
    {
        //TODO: address editor
    }
    else
    {
        @Value.FullNom(base.Lang())
    }

</div>

@code {
    [Parameter] public AddressModel Value { get; set; }
    [Parameter] public bool CanEdit { get; set; }
    private bool isEditing;
}
